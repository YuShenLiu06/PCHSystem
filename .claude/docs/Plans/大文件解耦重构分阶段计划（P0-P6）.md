# PCHSystem 大文件解耦重构 · 分阶段计划（终稿）

> 状态：终稿（4 视角审查 + 结构探索 + Plan agent 设计 + 5 处草案修正 + 用户拍板 2 项：bug 先修 P0 / service 层纳入 P6）。
> 依据：4 视角并行审查（层次 / 简化 / 效率 / 复用）+ 全仓结构探索 + Plan agent 对 5 热点文件与测试耦合面的逐项核验。
> 详细版（各阶段完整卡）见同目录附录：[`大文件解耦重构分阶段计划-附录（各阶段完整卡）.md`](./大文件解耦重构分阶段计划-附录（各阶段完整卡）.md)。
> 归档：2026-08-30 入库本目录；§⓪ 两任务已随分支 `bugfix/backend-integrityerror-import` 落地（P0 修复 + 本归档），P1–P6 待执行。

## ⓪ 本次会话执行范围（用户已缩小：只做两件事，P1–P6 仅归档不执行）

### 任务 A · 修复 IntegrityError 缺 import bug（即§三 P0）
- **文件**：`Backend/app/repositories/construction_repo.py`
- **改动**：顶部 import 区 `from sqlalchemy import bindparam, delete, func, select, text` 之后插入一行 `from sqlalchemy.exc import IntegrityError`（对齐项目惯例：`sheet_repo.py:19`、`api/identity.py:18`、`api/sheets/rows.py:11` 三处同款；且与 sheet_repo 一致放在 `sqlalchemy.dialects` 之前）。
- **不改**：`:1178` except 块本身、`:1191` 的 `raise`——只补名字解析，行为路径零变化（原先触发即 NameError，现在走注释里写明的 savepoint 兜底重试）。
- **验证**：`python3 -m py_compile` 语法过 + `grep -n "from sqlalchemy.exc import IntegrityError"` 确认落位；不跑全套 pytest（一行 import 修复，throwaway 栈成本不成比例，P1 重构开工时首轮全套测试自然覆盖）。
- **提交**：worktree 内独立 commit（见下方执行方式）。

### 任务 B · 重构计划归档到 `.claude/docs/Plans/`
- **落两份文件**（目录既有惯例是中文描述性命名，如「大文件拆分计划（A+B+C）.md」；P0–P6 编号体系与既有 A+B+C 计划区分）：
  1. `.claude/docs/Plans/大文件解耦重构分阶段计划（P0-P6）.md` —— 本计划文件全文，归档时把附录引用改为同目录附录文件名（消除对 `~/.claude/plans/` 会话路径的依赖）。
  2. `.claude/docs/Plans/大文件解耦重构分阶段计划-附录（各阶段完整卡）.md` —— Plan agent 详细版全文（各阶段完整卡 + 依赖图 + 检查表 + 风险表）。
- **归档后**：`ls` 确认两文件落位、字节数非零；不改目录内其他既有计划文件。
- **提交**：worktree 内独立 commit（见下方执行方式）。

### 执行方式（用户拍板：worktree 中做，最后 push）
1. **EnterWorktree** 建工作树（分支名 `bugfix/backend-integrityerror-import`）。⚠️ 项目 memory：本 repo worktree 默认基于 origin 旧 phase0——进入后先 `git reset --hard main` 对齐当前主干再动手。
2. 任务 A（bug 修复）→ py_compile + grep 验证 → **commit 1** `fix: 补 construction_repo IntegrityError 缺失 import`（只含 `Backend/app/repositories/construction_repo.py`，`git commit -- <paths>` 分步法）。
3. 任务 B（计划归档）→ **commit 2** `docs: 归档大文件解耦重构分阶段计划（P0-P6）`（只含 `.claude/docs/Plans/` 两新文件）。
4. **push**：`git push -u origin bugfix/backend-integrityerror-import`。`main` 只接受 PR 合入（CONTRIBUTING §1），push 后由用户决定 PR。⚠️ memory：VM 直连 GitHub 不稳（需宿主机代理）——push 失败则报告，退回用户手动 push。
5. 全程不起 docker 栈、不碰 dev 业务库（py_compile 宿主机即可；无前端构建，无需软链 node_modules）。


## Context

仓库热点集中在 **sheets 域**，生产代码 5 个文件超用户编码规范红线（800 行上限）：

| 文件 | 行数 | 病因（审查结论） |
|---|---|---|
| `McdrPlugin/pch_system/sheet_commands.py` | 1710 | ~25 个 handler 样板复写（~600 行纯样板）；静态菜单硬编码 104 行 |
| `Backend/app/repositories/sheet_repo.py` | 1531 | 无 service 层，状态机+级联+编排全压 repo；batch 双胞胎函数；测试代码混入 |
| `Backend/app/repositories/construction_repo.py` | 1295 | `submit_report` 单函数 297 行混 8 职责；五个子域装一个文件 |
| `Frontend/src/views/sheets/SheetEditor.vue` | 966 | ~290 行模板块逐字复制两份（623 行注释自认债务） |
| `Frontend/src/composables/useSheetDetail.ts` | 734 | 5 个关注点同居；toast/refresh 样板 12+4 处 |

另有测试大文件（test_sheet_repo.py 2245 / test_sheets_api.py 2135）——次要，见阶段划分。

## 一、结构探索关键事实（重构的"地形图"）

### 现成先例（照抄即可）
- **`Backend/app/api/sheets/` 子包**（2026-07-09 已落地）：`__init__.py` 21 行聚合挂 6 个子 router；`_shared.py` 389 行放包内私有共享；外部 import 路径不变。→ repo 拆分包模板。
- **`McdrPlugin construction_commands.py`（436 行）**：命令回调 / HTTP client / 渲染函数三分组织。→ sheet_commands 目标形态。
- **`Frontend views/sheets/sheetHelpers.ts`**（commit 3d8d87a 从 SheetEditor 抽出纯函数）。→ Vue 瘦身先例。
- `services/` 下 archive/parsing/markdown_render 已子包化。

### 耦合面 / blast radius
- `sheet_repo` 被 api/sheets 全 6 子模块 + services/archive + services/sheet_row_order 引用；**常量被以 `sheet_repo.XXX` 属性式引用**（拆包须保属性路径或全量改点）。
- `construction_repo` 与 `sheet_repo` 存在 lazy import 单向环（`_maybe_auto_join` :91 延迟导入），拆包须保持延迟方向。
- `sheet_commands.py` 唯一生产入口 = `McdrPlugin/pch_system/__init__.py:16-55` **38 个显式导入符号**；命令树注册在 `__init__.py:145-423`（Literal 子树，mode 由分支闭包烘入——拆模块必须保留该模式）。
- `SheetEditor.vue` 仅被 `router/index.ts:52` 懒加载引用；`useSheetDetail.ts` 仅被 SheetEditor 引用——**前端耦合面最小，最安全先动**。
- 测试直引：Backend 8 个测试文件 `patch.object(模块,...)`；McdrPlugin 7 个测试文件直访 `sc._私有符号`——拆分大面积碰测试。

### 6 项隐性契约（拆分保命清单）
1. `BATCH_REASON_*` 中文字面量与 McdrPlugin scanner.py **跨端逐字对齐**——搬动常量不得改值。
2. `SheetArchived.detail` 含「归档/archiv」子串，MCDR 靠子串嗅探识别。
3. 常量属性式引用（`sheet_repo.XXX`）遍布 api/sheets + services。
4. 测试 patch.object / `sc._私有符号` 直引。
5. `__init__.py` 38 个显式导入清单须同步维护。
6. sheet_repo ↔ construction_repo lazy import 环方向不可反转。

### 测试基建（每阶段验收手段）
- Backend：throwaway postgres:16 + 一次性容器（项目 memory 安全跑法）；`test_openapi_freeze.py` 冻结 OpenAPI——api 签名变更会被拦。
- McdrPlugin：`PYTHONPATH=McdrPlugin pytest McdrPlugin/tests -q`（宿主机）；_stubs 须同步新 import 的命令节点。
- Frontend：`npm run test:run`（vitest）。
- git 近 15 commit 全 chore/docs——**主干静默，重构窗口好**。

## 二、Findings 汇总库（跨报告去重后）

### A. 结构根因类（层次审查核心结论）
| # | 发现 | 出处 | 位置 |
|---|---|---|---|
| A1 | **sheets 域无 service 层**：行状态机、级联重算、贡献聚合、batch 决策编排、auto-join 副作用全在 repo；已规划的 settle 编排将无处复用 | altitude-3 | sheet_repo 全文件 |
| A2 | **construction 同病**：`submit_report` 297 行 service 编排在 repo（归因/开关/去重/封顶/SAVEPOINT 全混） | altitude-4 + simplify-F3 | construction_repo:526-822 |
| A3 | repo 跨 schema 互调写路径（sheet_repo:91 auto-join lazy import construction_repo；construction_repo:149 直查 Sheet 表） | altitude-9 | 同左 |
| A4 | repo 干表现层的活：display_name 组装 + CSV 序列化；**兜底文案已漂移**（`account#{id}` vs `账号#{id}`） | altitude-8 + simplify-F11 | sheet_repo:1307-1531 / construction_repo:186,216,413 |

### B. 跨端契约脆弱类（高优修复）
| # | 发现 | 出处 |
|---|---|---|
| B1 | 后端 409 无结构化错误码，MCDR 靠 `"归档" in detail` 子串嗅探——改文案即静默失效 | altitude-1 |
| B2 | RBAC 谓词三端三副本（后端权威 + Web canManage/isManager + MCDR 同构+名字兜底反 R-5） | altitude-2 |
| B3 | reason 中文字面量当协议：batch skip reason 逐字对齐 MCDR 折叠谓词；construction reason 中文直落 DB | altitude-5 |
| B4 | mode 语义（lock 绝对/progress 增量）三处副本 + MCDR `_sheet_done` 本地算业务差额（R-7 边缘） | altitude-7 |
| B5 | MCDR manager remove 客户端两步拼装（GET 列表反查 account_id 再 DELETE）——账号解析应后端做 | altitude-6 |

### C. 纯粹冗余类（不改行为的瘦身，最高杠杆）
| # | 发现 | 收益 | 出处 |
|---|---|---|---|
| C1 | SheetEditor 两分支 ~290 行模板逐字复制 → 抽 SheetMaterials.vue | **-460 行**（966→~500） | simplify-F1/F16 + altitude-10 |
| C2 | sheet_commands ~25 个 handler 样板 → 推广 `_submitc_dispatch` 先例为通用 dispatch 工厂 | **-600 行**（1710→~1100） | simplify-F2/F14 |
| C3 | `_batch_decide_lock`/`_batch_decide_progress` 同构双胞胎合并 | -75 行 | simplify-F4 |
| C4 | `update_row` 144 行五职责拆分 | 可测性 | simplify-F5 |
| C5 | `submit_report` 按天然接缝拆（_resolve_attribution/_aggregate/_prefetch/_decide_entry） | 可测性 | simplify-F3 |
| C6 | 死代码×3：`_disable_active_source`(construction_repo:910，0 调用)、`_sheet_list`(sheet_commands:293，未挂树)、`upsert_row`(sheet_repo:435，仅测试用) | -75 行 | simplify-F6 + altitude-12 |
| C7 | `_sheet_root` 104 行静态菜单 → 常量表/由注册表生成 | -90 行 | simplify-F7 |
| C8 | useSheetDetail：`withToast` 包装 12 处 toast 样板 + `refreshAndSync` 合并 4 处 | 防漏 sync | simplify-F8/F9 |
| C9 | SheetEditor「加入施工」70 行独立特性抽 composable/子组件 | shell 边界 | simplify-F10 |
| C10 | useSheetDetail 拆 `useSheetRowDrafts`（文件头 TODO 自认时机已到） | 734→~530 | simplify-F17 |
| C11 | web_account_repo lazy import 5 处函数内重复 → 顶部 import（construction_repo:40 已证明环不真实） | -重复咒语 | simplify-F12 |
| C12 | 行状态可见性谓词散在 composable+模板内联 ×2 → `rowActions(row)` 单一入口 | 与 C1 联动 | altitude-11 |

### D. 效率类（按影响排序）
| # | 发现 | 量级 | 出处 |
|---|---|---|---|
| D1 | `list_all_sheets_with_rows` 经典 N+1（1+2N 往返，名字解析纯丢弃） | N=50→101 查询 | eff-1 |
| D2 | MCDR `_find_row_or_tell` 每命令先拉整表再动作 = 2 次串行 HTTP；最常用命令族延迟翻倍 | 玩家可感 | eff-2 |
| D3 | `batch_submit`（MCDR 最热路径）list_rows 附带名字解析全丢弃 | 每次多 1 查询 | eff-3 |
| D4 | batch 内逐行 `_maybe_auto_join` 重复 3 组查询（B=20→60 次多余查询，全在 FOR UPDATE 事务内） | 锁持有时间↑ | eff-4+9 |
| D5 | 前端 3s 轮询无变化时仍全量替换 `sheet.value` → treeRows 重建 + 全表 vnode diff | 常驻页 CPU | eff-5 |
| D6 | `submit_report` 逐条 upsert → 按 (account,registry) 预聚合后 executemany | P×R 往返→1 | eff-6 |
| D7 | 模板 O(N) 方法调用叠加 O(N²)（some() 挂列 + canClaimRow 内 findParentMode 线扫） | 175 行×3s 复合 | eff-7 |
| D8 | `get_progress` 同表两次聚合+两次 briefs 解析+4 个 await 串行 | 轮询放大 | eff-8 |
| D9 | 级联路径逐子行往返（release 逐行 DELETE / update 逐子行 recompute） | C 子行=C 往返 | eff-11 |
| D10 | `close_all_participants` 逐实体 UPDATE → 单条 criteria UPDATE | 归档事务内 | eff-12 |
| D11 | MCDR `view` 分页每翻页重拉整表 | 浏览大表 | eff-13 |
| D12 | el-table 树模式无虚拟化（中期 el-table-v2，短期接受） | 历史事故量级 | eff-10 |

### 范围外顺带发现
- ⚠️ 正确性 bug：`construction_repo.py:1178 except IntegrityError` 的 `IntegrityError` 不在 import 列表——触发即 NameError。**已拍板：P0 先行单独修复（见§三），与重构彻底分离。**
- 文档漂移：Frontend/CLAUDE.md:77 写 1s 轮询，代码已 3s（并入 P1 D5 commit 顺带修）。
- 效率审查确认干净项：el-popover 事故修复无残留；MCDR @new_thread 全合规；rowEqual/SWR 缓存是全仓最佳实践。

## 三、分阶段方案

### 排序修正（Plan agent 对草案的 5 处纠偏）

1. **D7 并入 C12**（`rowActions(row)` 单一入口 + computed 化本就是 D7 的修法，O(N²) 随之消失）；**D5 留 P1 但独立 commit**（skip 替换有 stale-UI 风险，须独立验收）。
2. **C8+C10 提前进 P1**：前端一个 phase 全做完，落地即达红线（避免同端同测试套件拆两 phase 的中间态）。
3. **P2 必须含渲染抽出**：只做 in-file 瘦身 1710−600−90≈1010 仍超 800 红线；抽出 `sheet_render.py` 后主文件 ~700。
4. **测试耦合面实测纠偏**：真正 `patch.object(repo模块,...)` 仅 **1 处**（`test_construction_participants.py:487`）；7 个测试文件只是 import + 属性引用，经包 `__init__` 再导出全部存活。同步面比预估小一个量级。
5. **P3 拆 3a（纯瘦身）/ 3b（查询效率）**，验收口径分离；**A4 挪 P5**（改用户可见文本，属契约迁移不是瘦身）。

**P2 安全核心 = 38 个导出符号门面不动**：所有 handler 原名同签名、只改函数体（委托 dispatch 工厂），则 `__init__.py:16-55` 导入清单、`:145-423` 命令树、`_stubs.py`、7 个直引测试**全部零改动**。

### 阶段总览

| Phase | 端 | findings | 主文件行数变化 |
|---|---|---|---|
| **P0** bug 先修 | Backend | 范围外发现 | construction_repo +1 行 import |
| **P1** 前端达标 | Frontend | C1 C9 C12 D7 C8 C10 (+D5 独立 commit) | SheetEditor 966→~500；useSheetDetail 734→~480 |
| **P2** MCDR 达标 | McdrPlugin | C2 C7 C6(_sheet_list) + 渲染抽出 | sheet_commands 1710→~700；新 sheet_render ~350 |
| **P3a** 后端纯瘦身 | Backend | C3 C4 C5 C6(×2) C11 + 契约冻结测试 | sheet_repo 1531→~1400；construction_repo 1295→~1270 |
| **P3b** 后端查询效率 | Backend | D1 D3 D4 D9 D10 | 行数基本不变 |
| **P4** 后端拆包 | Backend | 子包化（照 api/sheets 模板） | 两 repo → 子包，各模块 100~400 |
| **P5** 跨端契约硬化 | 三端 | B1→D2、B2、B3、B5、A4 | 小幅增加（错误码/枚举表） |
| **P6** service 层根治 | Backend | A1 A2 A3 | repo 子包逻辑迁 service 子包；无新增超标文件 |
| Roadmap | — | B4、D6 D8 D11 D12、api 测试大文件、el-table-v2 | — |

P0–P4 结束时 5 个红线文件全部达标；P6 根治结构根因（用户拍板纳入）。

### 依赖图

```
P0 ─→ P1 前端 ─────────────────────────────┐
   ─→ P2 MCDR ──────────────────────────────┼─→ P5 契约硬化 ─→ P6 service 层
   ─→ P3a 后端瘦身 → P3b 查询效率 → P4 拆包 ─┘
```

- **可并行**：P1 ∥ P2 ∥ P3a（三端互不相交）。
- **串行**：P3a→P3b（先定型函数簇再动 SQL）→P4（瘦身后再拆，边界以瘦身后的函数簇为准）→P5（契约改动落在最终布局上）→P6（service 接缝 = P3a 拆好的私有函数 + P4 拆好的模块边界）。
- P5 内部 **B1→D2** 强串行；B2/B3/B5/A4 相互独立各一 PR。

### 各阶段卡（精简版，完整版见附录）

#### P0 · IntegrityError 缺 import 修复（先行，与重构彻底分离）
`Backend/app/repositories/construction_repo.py:1178` 的 `except IntegrityError:` 引用了未 import 的名字（已 grep 验证 exit=1），join_construction 并发兜底触发即 NameError→500。修法：顶部补 1 行 import（`from sqlalchemy.exc import IntegrityError`），加一条触发该兜底路径的单测（可选，若无现成并发测试基建则只做 import 修复 + 人工核对），**单独 commit，先于一切重构落地**。

#### P1 · 前端两文件达标（每步一 commit）
**改动**：`SheetEditor.vue`（966→~500）、新建 `SheetMaterials.vue`（~300，两分支共用树状表格）、新建 `useJoinConstruction.ts`（~80）、`useSheetDetail.ts`（734→~480）、新建 `useSheetRowDrafts.ts`（~220）、`sheetHelpers.ts`（+行状态谓词）。
1. **C1**：`SheetMaterials.vue` 收敛两份逐字复制的树状表格（~L373-520 与 el-tabs 分支）；props 传 `treeRows/columnWidths/canEdit/...`，编辑动作 event 上抛；**`popoverMounted` 惰性挂载状态随迁**（el-popover 事故修复不可回退）。
2. **C9**：`joinButton*` 三态按钮块（L288-308）抽 `useJoinConstruction.ts`。
3. **C12+D7**：行状态可见性谓词收敛 `rowActions(row)` 单一入口，纯谓词进 `sheetHelpers.ts`；per-row flags computed 预计算进 treeRows，消模板 O(N) 方法调用 + `findParentMode` 线扫。
4. **C8**：`withToast` 包装 12 处 toast 样板 + `refreshAndSync` 合并 4 处（防漏 sync）。
5. **C10**：拆 `useSheetRowDrafts.ts`（行内编辑态 + 子物品操作两节）；handle 返回面不变 → SheetEditor 零改动。
6. **D5（独立 commit，最后落）**：`load()` 数据未变时（deep-equal / updated_at 守卫）跳过 `sheet.value` 全量替换；补「编辑后 UI 仍更新」用例；顺带修 `Frontend/CLAUDE.md:77` 轮询间隔文档漂移。
**验收**：`cd Frontend && npm run test:run && npm run build`；`wc -l` 对照估算。
**风险**：C1 props 边界漏传 → build+既有 spec 兜；D5 stale-UI → 独立 commit + 专属用例。

#### P2 · MCDR sheet_commands 达标
**改动**：`sheet_commands.py`（1710→~700）、新建 `sheet_render.py`（~350）、`test_sheet_commands_render.py`（删死代码用例）。**零改动**：`__init__.py`、`_stubs.py`。
1. **C2**：`_submitc_dispatch`（:1651）先例推广为通用 dispatch 工厂（参数化 thread 名 / ctx 取值键 / client 调用 lambda / 回执渲染 lambda），~25 个 handler 改薄壳委托。铁律：38 个导出符号原名同签名。
2. **渲染抽出 + C7**：`_sheet_root`（185-288）、`_render_sheet_detail`、`_build_submit_receipt`、`_format_manager_list` 迁 `sheet_render.py`；菜单改 `_MENU_GROUPS` 常量表 + 渲染循环，**输出 RTextList 逐字节等价**（render 测试 837 行是现成基线）。
3. **C6**：删 `_sheet_list`（:293，未挂命令树）+ 同步删对应测试用例。
本阶段**不触碰任何 reason/SHEET_* 文案常量值**（契约 1）。
**验收**：`PYTHONPATH=McdrPlugin pytest McdrPlugin/tests -q` 全绿；`git diff --exit-code McdrPlugin/pch_system/__init__.py McdrPlugin/tests/_stubs.py` 必须为空；`wc -l` 两文件。

#### P3a · 后端文件内纯瘦身（每步一 commit）
1. **C11**：sheet_repo 五处函数内 lazy import web_account_repo 提顶部（construction_repo:40 已证明环不真实）。
2. **C6-a**：删 `construction_repo._disable_active_source`（:910，0 调用）。
3. **C6-b**：删 `sheet_repo.upsert_row`（:435，生产零调用——api `rows.py:417` 是同名端点函数，走 create_row/update_row）。测试迁移逐条映射等价断言，删一例登记一条，防覆盖丢失。
4. **C3**：合并 `_batch_decide_lock`/`_batch_decide_progress` 双胞胎（−75 行）；**先写参数化 reason 分支矩阵测试再合并**。
5. **C4**：`update_row`（144 行五职责）纯提取私有子函数，逻辑零改动。
6. **C5**：`submit_report` 按天然接缝提取 `_resolve_attribution`/`_prefetch_sheet_rows`/`_decide_entries`/`_aggregate_and_upsert`。
7. **新增 `Backend/tests/test_cross_contract_literals.py`**：同仓读两端源码，断言 `BATCH_REASON_*`/`REASON_*`/lock skip reason 逐字相等——把契约 1 从注释升级为测试。
**验收**：throwaway postgres 全套（见§四）+ `test_openapi_freeze.py` 不重生成直接过 + openapi diff 为空。

#### P3b · 查询效率（每项独立 commit + query-count 断言）
1. **D1** `list_all_sheets_with_rows` 1+2N→常数查询（selectinload/joinedload）；实现前核实「名字解析纯丢弃」断言。
2. **D3** `batch_submit` 路径 `list_rows` 免名字解析（MCDR 最热路径）。
3. **D4** batch 循环内 `_maybe_auto_join` 逐行 3 组查询 → 循环前按 account 批量预取。红线：只合并同事务相同 key 重复查询，**不改锁获取模式与范围**（FOR UPDATE 事务内）。
4. **D9** 级联批量化：release 逐子行 DELETE→单条 where；update 级联取数一次取全。
5. **D10** `close_all_participants` 逐实体 UPDATE→单条 criteria UPDATE。
6. 新增 sqlalchemy 事件监听 query-count 断言测试。
**风险（本阶段最大）**：批量预取改锁时序 → query-count + 既有 batch 测试双兜底。

#### P4 · 两 repo 子包化（照抄 `Backend/app/api/sheets/` 模板）
1. **sheet_repo/ 子包**：`constants.py`（STATUS/MODE/BATCH_REASON/异常类）+ 读查询 + 行写 + batch 族 + 级联/advance + CSV/display 各一模块；`__init__.py` **全量再导出含 `_` 私有名与常量**——保契约 3（`sheet_repo.XXX` 属性式引用遍布 api/sheets + services + 7 测试文件）与 `from ...sheet_repo import SheetArchived` 零改动。
2. **construction_repo/ 子包**：按现有五分节（settings/归因聚合/上报写/源切换/participants）各一模块 + 全量再导出（`_get_active_participant_row` 有 1 处测试直引）。
3. **契约 6**：sheet_repo 子模块对 construction_repo 的延迟 import 保持在函数体内；不反向顶层 import。
4. 验证唯一 patch 点 `test_construction_participants.py:487`；失效则改 patch 定义子模块（唯一预期测试改动）。
**验收**：throwaway postgres 全套 + 逐 api 模块 import 冒烟（`python -c "from app.api import sheets, construction, scoring"`）防迟发 ImportError。

#### P5 · 跨端契约硬化（全部 additive，先落后端后落 MCDR/前端，旧端必须继续工作）
1. **B1 结构化错误码**：409/422 加 `code` 字段（如 `SHEET_ARCHIVED`），**原中文 detail 不动**；MCDR 改优先读 code、子串兜底保留一版本；regen openapi + 更新 api 文档。
2. **D2（依赖 B1）**：`_find_row_or_tell` 删预检整表 GET——最常用命令族 2 次串行 HTTP→1 次。
3. **B2**：detail 响应加 `viewer_permissions` 对象；Web `canManage/isManager` 改读之（兑现 R-9）；MCDR `_is_manager` 名字兜底删除（修正 R-5 边缘）。
4. **B3**：reason 收敛显式枚举表，值不变；字面量测试升级枚举镜像断言；更新 submit-extension/construction 文档。
5. **B5**：DELETE `/sheets/{id}/managers` 增收 `player_uuid`（后端解析 account），旧参保留；MCDR 撤销 GET 反查两步拼装。
6. **A4**：`account#{id}` vs `账号#{id}` 按 api 文档定权威值统一（唯一改可见文本子项）。
**发布顺序**：每子项 backend PR 先部署，MCDR/前端后发（热重载即可）。

#### P6 · service 层根治（用户拍板纳入；接缝 = P3a 拆好的函数 + P4 拆好的模块）
**目标**：业务规则上提 service，repo 退回纯数据访问；A3 repo 跨 schema 互调环消解。service 层的家已存在——`Backend/app/services/`（archive/parsing/markdown_render/sheet_row_order 惯例照抄）。
**边界原则**：**写路径必经 service**（状态机决策/编排/副作用）；**读路径 api 可直查 repo**（避免一次性全改调用链）。
1. **P6a sheets_service**（子包，模板同 api/sheets）：sheet_repo 子包中 advance/claim/release/reject 状态机、`update_row` 业务校验、batch 决策、`_maybe_auto_join` 上提 `services/sheets_service/`；CSV 序列化 + display_name 组装迁 `api/sheets/_shared.py`（表现层回归表现层，落实 A4 归属）；api/sheets 6 子模块写路径改调 service。
2. **P6b construction_service**：`submit_report` 四接缝函数（P3a C5 已拆）整体上提 `services/construction_service.py`；归因/封顶/去重决策在 service，upsert/查询留 repo。
3. **A3 环消解**：`_maybe_auto_join` 迁入 service 后 service 顶层 import 两 repo——**repo 间 lazy import 环消失**（契约 6 消亡，成为本步验收证据）。
4. **测试迁移**：`test_sheet_repo.py`（2245 行）行为断言迁 `test_sheets_service.py`，逐例登记映射（手法同 P3a upsert_row）；`test_sheets_api.py`（2135 行）应零改动（api 签名不变）。
**验收**：throwaway postgres 全套 + openapi freeze 空 diff + `grep -rn "_maybe_auto_join\|maybe_auto_join" Backend/app/repositories/` 为空 + 行数台账（service 子包模块 100~400）。
**风险**：工程量最大阶段 → P6a/P6b 各自独立 PR 先后落；api 直读 repo 的读函数保留不动，把重接线范围压到写路径。

## 四、验证方式（全局不变量检查表，每阶段收尾必跑）

**三端测试**
- Backend（P0 起每 PR）：throwaway postgres 全套（memory `backend-tests-throwaway-postgres` 跑法：postgres:16 一次性容器 + 一次性 backend 容器挂 `Backend/app`+`Backend/tests`、pip 装 pytest/httpx、`alembic upgrade head`；**绝不碰 dev 业务库**——conftest 会 TRUNCATE）。
- MCDR（P2 起每 PR）：`PYTHONPATH=McdrPlugin pytest McdrPlugin/tests -q`（宿主机，`_stubs.py` 免真 mcdreforged）。
- Frontend（P1 起每 PR）：`cd Frontend && npm run test:run && npm run build`。

**P2 门面纪律（专项）**
- `git diff --exit-code McdrPlugin/pch_system/__init__.py McdrPlugin/tests/_stubs.py` —— 必须为空。

**OpenAPI 冻结**
- P0–P4 全程：`git diff --stat Backend/openapi.json` 为空（`test_openapi_freeze.py` 不重生成直接过）；仅 P5 各子项 PR 内 regen（regen 与代码同 commit）。

**跨端契约 grep（P1–P4 每阶段收尾 + P5 每子项前后）**
```bash
grep -n 'BATCH_REASON_READY = "已备齐"' Backend/app/repositories/sheet_repo*.py Backend/app/repositories/sheet_repo/*.py 2>/dev/null
grep -n 'BATCH_REASON_NO_ITEM = "背包没有此物"' Backend/app/repositories/sheet_repo*.py Backend/app/repositories/sheet_repo/*.py 2>/dev/null
grep -n 'REASON_NO_ITEM = "背包没有此物"\|REASON_READY = "已备齐"' McdrPlugin/pch_system/scanner.py
grep -rn '归档' Backend/app/repositories/sheet_repo*.py | grep -i 'archiv'   # SheetArchived.detail 子串契约（契约 2）
```
P3a 起固化为 `test_cross_contract_literals.py`，grep 退居快速手验。

**行数红线台账**：每阶段收尾 `wc -l` 记入 PR 描述，对照总览表估算。

## 五、风险登记

| Phase | Top 风险 | 缓解 |
|---|---|---|
| P1 | C1 props/events 边界漏传（popoverMounted 惰性挂载回退）；D5 stale-UI | build+既有 spec 兜 C1；D5 独立 commit + 专属用例；单 commit revert |
| P2 | 菜单/回执渲染输出漂移；38 符号门面被顺手"清理" | render 测试 837 行字节基线；门面 diff 断言进检查表 |
| P3a | upsert_row 测试迁移丢覆盖；C3 合并漏 reason 分支 | 迁移映射逐例登记；合并前先写参数化分支矩阵测试 |
| P3b | 批量预取改锁时序（FOR UPDATE 事务内） | 只合并相同 key 重复查询不改锁模式；query-count + 既有测试双兜底 |
| P4 | 子包间漏 import 迟发 ImportError；唯一 patch.object 失效 | 逐 api 模块 import 冒烟；patch 点专门验证 |
| P5 | 三端发布顺序错配（MCDR 先于后端读到不存在的 code） | additive 保旧端兼容；backend 先部署、MCDR/前端后发 |
| P6 | 调用链重接线工程量大；2245 行测试搬迁丢覆盖 | P6a/P6b 独立 PR；读路径不动只重接写路径；测试迁移逐例登记 |

全局背景：后端测试已知 flakiness（conftest TRUNCATE 死锁，memory 有记录）——验收失败先 `pytest --lf` 复跑判别，勿误判为重构引入。

## 六、执行方式与不做清单

**执行方式**：每阶段一个分支（用户惯用 worktree，起栈前查项目级 `worktree-stack-migrate` skill）；每步一 commit（分步 commit 用 `git commit -- <paths>`）；全部通过后再由用户手动 push（GitHub 走宿主机代理，勿自动跑 git 网络命令）。

**不做（YAGNI 边界）**：
- **B4** mode 语义三端统一 + MCDR `_sheet_done` 本地算业务差额（R-7 边缘）——涉及玩法语义拍板，独立提案。
- **D6** submit_report 预聚合 executemany、**D8** get_progress 并行化、**D11** view 分页本地缓存、**D12** el-table-v2 虚拟化。
- `test_sheets_api.py`（2135 行）拆分——P6 后再议（P6a 已顺带拆走 repo 行为测试）。
- 前端 `Identities.vue`、wiki、积分层等 CLAUDE.md 待办——与本次解耦无关。
