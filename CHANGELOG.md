# Changelog

本项目所有显著变更记录于此文件。

格式遵循 [Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [Semantic Versioning 2.0.0](https://semver.org/lang/zh-CN/)。
三端（后端 · 游戏端 · 前端）各自独立维护版本号。

> **阅读约定**：`[Unreleased]` 段面向开发者，可含实现细节与提交引用；
> 已发版段落（`vX.Y.Z`）面向**使用者**，只讲「能感知到什么变化」，用自然语言、尽量精简。

---

## [Unreleased]

> 下一版本待归档。新增条目按组件 × Added/Changed/Fixed/Security 分类补在此处。
> 发版时浓缩为面向使用者的自然语言，固化为 `## [<组件>-vX.Y.Z] - YYYY-MM-DD` 段并重置本段（详见底部「版本化策略」）。

### Backend

- **Fixed**：sheet 行改名不再重复建行（issue #20）。`PUT /sheets/{id}/rows` 改为单端点按 `row_id` 分流——带 `row_id` 按主键更新（可改名，字段部分更新），不带 `row_id` 严格新建。`item_name` 从 upsert 锁点降级为普通数据字段，修改操作统一以 `id` 为定位主轴。
- **Fixed**：新建同名行不再静默覆盖旧行（严格 INSERT，撞 `UNIQUE(sheet_id, item_name)` → 409 中文提示「物品名重复」）。
- **Changed**：archived 终态写操作 409 文案改中文（「项目已归档，只读」）。

### Frontend

- **Changed**：行编辑保存带 `row_id`（走按主键更新路径，改名生效且不再重复）；`RowUpsertRequest` 增可选 `row_id`。

### McdrPlugin

- **Changed**：`!!PCH sheet set` 改为按行号（`row_id`）更新 need/排序，与 `delrow`/`claim` 等命令一致（id 主轴），不再按物品名 upsert。`setreg` 同改按 `row_id` 更新；`add` 保持按物品名严格新建（同名→回执报错）。

---

## [backend-v0.5.0] - 2026-07-07

### Added

- **解析 Create 蓝图**：上传 `.nbt` 蓝图文件（机械动力模组 / 原版结构），系统自动解析材料清单并翻译成中文名，预览与生成项目流程同投影文件。
- （运维）**一键安装 / 更新脚本**：首次部署一条命令完成（检测安装 Docker、国内网络自动选镜像、生成配置、起服务 + 跑数据库迁移、构建前端、装好游戏端插件并填好令牌）；后续更新按代码改动智能决定重建哪些容器、迁移前自动备份数据库、校验游戏端令牌一致性、拒绝在未提交改动上运行。

---

## [frontend-v0.5.0] - 2026-07-07

### Added

- **解析 Create 蓝图**：投影 / 蓝图上传页新增 `.nbt` 文件支持，按扩展名自动选择解析方式，预览与生成项目流程不变。

---

## [backend-v0.4.1] - 2026-07-07

### Fixed

- 修复上传 Create（机械动力）蓝图经投影 mod 转出的 `.litematic` 时解析失败、整份材料清单被丢弃的问题；解析失败时会提示可读的中文原因（#8）。

---


## [backend-v0.4.0] - 2026-07-06

### Added

- 项目归档自动生成**贡献占比饼图**，直观展示每位参与者贡献了多少材料。
- 归档文件改为**按项目独立文件夹**存放（说明文档 + 贡献图集中在一起），便于查找。
- 支持**记住玩家上次打开的项目**，供游戏端「快速重开」使用。
- 项目列表可按请求把**自己参与的项目排在前面**。
- （运维）wiki 内容同步改为**经 git 仓库双向同步**，默认关闭，需配置 `WIKI_GIT_*` 环境变量开启。

---

## [htcmc_auth-v0.4.0] - 2026-07-06

### Added

- **一键提交**：查看项目时点按钮即可扫描整个背包，把匹配的材料一次性上交（支持潜影盒内的物品）。
- **快速重开上次项目**：直接输入 `!!sheet` 重开上次查看的项目，或 `!!sheet <编号>` 打开指定项目；`!!PCH sheet last` 同效。
- **更聪明的项目列表**：默认只显示进行中的项目；自己参与的（作为创建者 / 认领人 / 贡献者）排在最前；每行标注所处阶段（收集中 / 建造中）。
- **列表筛选简写旗标**：`!!sheet list` 支持 `-m`(我的) / `-c`(收集中) / `-t`(建造中) / `-a`(已归档) / `-l`(全部)，可组合如 `-ma`。
- **拥有者改物品 ID**：项目拥有者可在行上点「改ID」直接修改材料对应的游戏物品（缺省读取手持物品）。
- **查看界面美化**：物品列表新增分节标题，按钮右对齐、底栏按钮居中，显示更整齐。

### Fixed

- 空项目（无任何物品行）不再错误显示「一键提交」按钮，并补回物品列表的分节标题。

---

## [frontend-v0.4.0] - 2026-07-06

### Added

- 项目归档预览中**显示贡献占比图**，与后端生成的饼图同步。

---

## [backend-v0.3.0] - 2026-07-03

### Added

- **投影文件一键生成材料清单**：上传 `.litematic` 投影文件，系统自动解析其中的方块并翻译成中文名，一键生成项目材料清单（支持原版与 Create 模组物品）。
- **多人上报进度**：材料行支持多人累计上交，不再限定单人认领。
- **统一通知系统**：认领 / 交付 / 打回 / 上交等事件会自动生成通知，游戏端玩家上线即可收到。
- **游戏端代玩家操作**：游戏端的命令可以代表玩家执行写操作（如提交材料），权限与 Web 端完全一致。

---

## [mcdr-v0.3.0] - 2026-07-03

### Added

- **`!!PCH sheet` 命令树**：游戏内全套项目管理命令（查看 / 认领 / 交付 / 打回 / 解除 / 上交等共 14 条）。
- **通知轮询**：玩家在线时自动接收通知，离线期间的通知在上线时补发。
- **可点击的聊天按钮**：项目查看界面里的操作改成可点击按钮，点一下自动填好对应命令。

### Changed

- **主菜单视觉重做**：`!!PCH` 帮助菜单重新设计，命令分类清晰、可点击。

### Fixed

- 修复命令参数被强制要求输入导致「未知命令」、通知文案缺项目名、轮询间隔配置漂移等问题。

---

## [frontend-v0.3.0] - 2026-07-03

### Added

- **投影上传页**：浏览器端上传 `.litematic`，预览解析结果并勾选生成项目。
- **自动刷新**：项目列表与详情页自动轮询刷新，正在编辑的草稿不会丢失。
- **进度上交 / 调整**：上报式材料的上交入口，以及拥有者调整进度的入口。

### Fixed

- 修复拥有者在自己项目里看到重复按钮的问题。

---

## [v0.2.0] - 2026-07-02

### Added

- **材料清单协作**：材料行支持认领 / 标记备齐 / 交付 / 打回 / 解除，Web 端与游戏端对等操作；列表与详情显示认领人的游戏名。

---

## [v0.1.0] - 2026-07-02

首个可用版本，三端骨架与核心登录链路打通。

### Added

- **后端**：FastAPI + PostgreSQL 骨架 + 数据库迁移机制；**登录鉴权链路**（游戏端 `!!PCH login` → 浏览器兑换进入后台）；材料清单基础增删改查与 CSV 导出。
- **游戏端（MCDR 插件）**：插件骨架 + `!!PCH login` 登录命令（可点击链接）。
- **前端**：Vue3 后台骨架 + 登录兑换页 / 个人信息页 / 材料清单表格（增删改查）。
- **工程**：根规范（红线 R-1~R-12 / 命名 / 分布式文档体系）+ Docker Compose 部署 + 集成测试服。

---

## 版本化策略

三端各自独立 SemVer，按 [`CONTRIBUTING.md`](./CONTRIBUTING.md) §4 维护。

| 组件 | tag 约定 |
|---|---|
| 后端 | `backend-vX.Y.Z` |
| 游戏端（MCDR 插件） | `htcmc_auth-vX.Y.Z`（v0.4.0 起改名，符合 MCDR PluginCatalogue 合法格式；历史 `mcdr-v0.3.0` 前向兼容保留） |
| 前端 | `frontend-vX.Y.Z` |

发版流程：

1. 把 `[Unreleased]` 段浓缩为面向使用者的自然语言（去掉实现细节与提交哈希）。
2. 固化为 `## [<组件>-vX.Y.Z] - YYYY-MM-DD` 段，置顶于已发版段落上方。
3. 重置 `[Unreleased]` 段为空占位。
4. 打对应 tag。

> MCDR tag 改名背景见 [`Docs/Reports/mcdr-publishing-strategy.md`](./Docs/Reports/mcdr-publishing-strategy.md)。
