# TODO 代办清单

- [ ] fix(sheet): submit 的提交范围应该是mode == progress | mode == lock & 自己为认领人 的情况，当前状况为自己是认领人反而不能一键提交
- [ ] feat(sheet): 将 view 的默认的新增物品切换为 addhand 命令
- [ ] setreg 在游戏中默认切换为切换为手上物品的id，当然也支持主动输入参数(该参数为空时走手上物品id)