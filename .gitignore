# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
build/
dist/
.pytest_cache/

# MCDR 打包产物（mcdreforged pack 本地产物，不入库；CI/release 在远端产出）
*.mcdr

# Node
node_modules/
.vite/

# Env & secrets
.env
.env.local

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
McdrPlugin/pch_system/config.json

# 本地临时文件（如 ./Run 测试产物）
/Run
/tempt/
.mcp.json

# Docker Compose 本地 override（含机器特定绝对路径，勿提交）
docker-compose.override.yml

# PCHSystem 部署脚本产物（由 Scripts/install.sh + update.sh 生成）
.pchsystem.deploy.env
backups/
# Claude worktrees（本地 git worktree 检出，含机器绝对路径与大量文件，勿入库）
.claude/worktrees/
